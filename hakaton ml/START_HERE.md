# 🎯 НАЧНИТЕ ЗДЕСЬ!

## Быстрая инструкция по запуску проекта

---

## ✅ ШАГ 0: Проверка готовности

Перед запуском проверьте, что все готово:

```bash
python3 check_ready.py
```

Если все ✅ - можно начинать!

---

## 📋 ПОРЯДОК ЗАПУСКА

### 1️⃣ Подготовка данных (Никита)

```bash
python3 src/data_preparation.py
```

**Что делает:**
- Анализирует данные
- Создает признаки
- Проверяет на утечки
- Готовит финальные датасеты

**Время:** ~5-10 минут  
**Результат:** `data/processed/train_final.parquet` и `test_final.parquet`

---

### 2️⃣ Обучение Baseline модели

```bash
python3 src/baseline.py
```

**Что делает:**
- Обучает логистическую регрессию
- Выводит базовые метрики

**Время:** ~1-2 минуты  
**Результат:** `models/baseline.pkl`

---

### 3️⃣ Обучение CatBoost модели (ОСНОВНАЯ)

```bash
python3 src/model_catboost.py
```

**Что делает:**
- Обучает градиентный бустинг
- Показывает важность признаков
- Выводит метрики

**Время:** ~5-10 минут  
**Результат:** `models/catboost_model.pkl`

---

### 4️⃣ Обучение LightGBM модели (опционально)

```bash
python3 src/model_lightgbm.py
```

**Что делает:**
- Обучает альтернативную модель
- Для сравнения с CatBoost

**Время:** ~5-10 минут  
**Результат:** `models/lightgbm_model.pkl`

**Примечание:** Можно пропустить, если не работает

---

### 5️⃣ Создание Submission файла

```bash
python3 src/inference.py
```

**Что делает:**
- Загружает лучшую модель
- Делает предсказания на тесте
- Создает submission.csv

**Время:** ~1 минута  
**Результат:** `output/submission.csv` ← **ЭТОТ ФАЙЛ ОТПРАВЛЯТЬ НА ХАКАТОН!**

---

## 🎯 ИТОГОВЫЙ ФАЙЛ

После выполнения всех шагов ваш файл для отправки:

```
output/submission.csv
```

**Формат:**
```csv
ID,выпуск
116556,1
116591,0
...
```

---

## ⚡ Быстрый запуск (все сразу)

Если хотите запустить все последовательно:

```bash
python3 src/data_preparation.py && \
python3 src/baseline.py && \
python3 src/model_catboost.py && \
python3 src/inference.py && \
echo "✅ Готово! Проверьте output/submission.csv"
```

---

## 📊 Проверка результатов

```bash
# Проверить submission
python3 -c "
import pandas as pd
sub = pd.read_csv('output/submission.csv')
print(f'✅ Строк: {len(sub)}')
print(f'✅ Колонки: {list(sub.columns)}')
print(f'✅ Нет NaN: {sub.isnull().sum().sum() == 0}')
print(f'Распределение:')
print(sub['выпуск'].value_counts())
"

# Посмотреть логи
tail -50 output/logs.txt
```

---

## 🆘 Если что-то пошло не так

### Ошибка: "Файл не найден"
→ Запустите шаги по порядку, начиная с `data_preparation.py`

### Ошибка: "ModuleNotFoundError"
→ Установите зависимости: `pip3 install -r requirements.txt`

### Ошибка: "LightGBM недоступен"
→ Можно пропустить, используйте только CatBoost

### Ошибка загрузки данных
→ Проверьте, что файлы в `data/raw:/` или `data/raw/`

---

## 📝 Чек-лист

- [ ] Шаг 1: Подготовка данных выполнена
- [ ] Шаг 2: Baseline обучена
- [ ] Шаг 3: CatBoost обучена
- [ ] Шаг 4: LightGBM обучена (опционально)
- [ ] Шаг 5: Submission создан
- [ ] Файл `output/submission.csv` проверен

---

## 🎉 Готово!

Теперь у вас есть:
- ✅ Обученные модели
- ✅ Файл submission.csv
- ✅ Все логи и метрики

**Удачи на хакатоне!** 🚀

---

## 📚 Дополнительная документация

- `QUICKSTART.md` - подробная инструкция
- `WORKFLOW.md` - описание workflow
- `INSTRUCTIONS.md` - детальные инструкции
- `LIGHTGBM_SETUP.md` - настройка LightGBM

