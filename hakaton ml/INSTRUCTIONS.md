# Инструкции по выполнению задачи

## 👥 Разделение задач

Проект разделен на две части:
- **Никита (Data + Features)**: Подготовка данных и создание признаков
- **Дима (Models + Validation)**: Обучение моделей и валидация

Подробный workflow см. в `WORKFLOW.md`

## 🚀 Быстрый старт

### Шаг 1: Установка зависимостей
```bash
pip install -r requirements.txt
```

### Шаг 2: Варианты запуска

**Вариант А: Полный пайплайн (Никита → Дима)**

```bash
# 1. Никита: Подготовка данных (все 8 шагов)
python src/data_preparation.py

# 2. Дима: Обучение моделей
python src/baseline.py
python src/model_catboost.py
python src/model_lightgbm.py

# 3. Дима: Предсказания
python src/inference.py
```

**Вариант Б: Пошаговый запуск (для отладки)**

```bash
# 1. Загрузка и анализ данных
python src/load_data.py

# 2. Предобработка данных
python src/preprocess.py

# 3. Создание признаков
python src/feature_engineering.py

# 4. Разделение на train/test
python src/split_train_test.py

# 5. Обучение baseline модели
python src/baseline.py

# 6. Обучение CatBoost модели
python src/model_catboost.py

# 7. Обучение LightGBM модели
python src/model_lightgbm.py

# 8. Предсказания на тесте
python src/inference.py
```

## 📋 Чек-лист выполнения задач

### ЧАСТЬ 1: Задачи Никиты (Data + Features)

1. ✅ Проанализировать data.csv
2. ✅ Присоединить marking.csv
3. ✅ Удалить студентов теста (выпуск=None) для обучения
4. ✅ Проверить отсутствующие данные
5. ✅ Построить список безопасных фич
6. ✅ Построить сами фичи
7. ✅ Проверить на утечки
8. ✅ Передать финальный датасет Диме

**Запуск:** `python src/data_preparation.py`

### ЧАСТЬ 2: Задачи Димы (Models + Validation)

### ✅ Задача 1: Разделить данные train/test по логике "до/после 2 курса"
- **Файл**: `src/split_train_test.py`
- **Логика**: Train = семестры 1-4, Test = студенты из sample_submission.csv
- **Статус**: ✅ Реализовано

### ✅ Задача 2: Построить baseline (логистическая регрессия)
- **Файл**: `src/baseline.py`
- **Модель**: LogisticRegression с балансировкой классов
- **Статус**: ✅ Реализовано

### ✅ Задача 3: Обучить CatBoost / LightGBM
- **Файлы**: `src/model_catboost.py`, `src/model_lightgbm.py`
- **Статус**: ✅ Реализовано

### ✅ Задача 4: Настроить валидацию
- **Файл**: `src/validation.py`
- **Методы**: Кросс-валидация, разделение train/val, метрики (ROC-AUC, Accuracy, F1)
- **Статус**: ✅ Реализовано

### ✅ Задача 5: Проверить признаки на даталики
- **Файл**: `src/utils.py` (функция `check_data_leakage`)
- **Проверки**: 
  - Уникальные значения в тесте, которых нет в трейне
  - Различия в распределениях
- **Статус**: ✅ Реализовано

### ✅ Задача 6: Прогнать тестовую выборку
- **Файл**: `src/inference.py`
- **Статус**: ✅ Реализовано

### ✅ Задача 7: Выгрузить результат в sample_submission.csv формат
- **Файл**: `src/inference.py` (функция `create_submission`)
- **Выход**: `output/submission.csv`
- **Статус**: ✅ Реализовано

## 🔍 Важные моменты

### Проверка на даталики
После создания признаков обязательно запустите проверку:
```python
from src.utils import check_data_leakage
from src.validation import validate_features

# В коде моделей уже есть проверка
```

### Настройка моделей
- **CatBoost**: параметры в `src/model_catboost.py` (можно настроить iterations, learning_rate, depth)
- **LightGBM**: параметры в `src/model_lightgbm.py` (можно настроить num_leaves, learning_rate)

### Выбор модели для inference
В `src/inference.py` измените:
```python
model_name = 'catboost'  # или 'baseline', 'lightgbm'
```

### Ансамбль моделей
В `src/inference.py` есть функция `ensemble_predictions` для усреднения предсказаний нескольких моделей.

## 📊 Структура данных

### data.csv
- PK: ID студента
- SEMESTER: семестр (1-4 для train)
- DNAME: название дисциплины
- TYPE: тип оценки (экз, зач, прак, КП)
- MARK: оценка (5, 4, 3, з, ня)
- GRADE: буквенная оценка (A, B, C)
- BALLS: баллы
- EXAM_DATE: дата экзамена

### marking.csv
- ИД: ID студента
- Факультет, Направление, год поступления
- статус: 1 (учится), -1 (отчислен), 2 (ак. отпуск)
- выпуск: целевая переменная ("выпустился" / "отчислен")

### sample_submission.csv
- ID: ID студента для теста
- выпуск: предсказание (1 = выпустился, 0 = отчислен)

## 🐛 Решение проблем

### Ошибка импорта
Убедитесь, что вы находитесь в корневой директории проекта и все зависимости установлены.

### Ошибка загрузки данных
Проверьте, что файлы находятся в `data/raw:/` (с двоеточием) или в `data/raw/`.

### Ошибка памяти
Если данных слишком много, можно:
1. Уменьшить количество итераций в моделях
2. Использовать sampling для feature engineering

## 📝 Логи

Все логи сохраняются в `output/logs.txt`. Проверяйте их для отладки!

## ⏱️ Примерное время выполнения

- Загрузка данных: ~1 мин
- Предобработка: ~2 мин
- Feature engineering: ~5-10 мин (зависит от объема данных)
- Разделение train/test: ~1 мин
- Baseline обучение: ~2 мин
- CatBoost обучение: ~5-10 мин
- LightGBM обучение: ~5-10 мин
- Inference: ~1 мин

**Итого**: ~30-40 минут на полный пайплайн

## 🎯 Следующие шаги для улучшения

1. **Гиперпараметрическая оптимизация**: использовать Optuna для подбора параметров
2. **Дополнительные признаки**: 
   - Взаимодействия между признаками
   - Временные признаки (дни между экзаменами)
   - Статистики по дисциплинам
3. **Ансамбль**: усреднение предсказаний всех моделей
4. **Калибровка**: калибровка вероятностей для лучшего порога

