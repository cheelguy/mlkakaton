# 🚀 Быстрый старт проекта

## Пошаговая инструкция по запуску

### Шаг 0: Проверка окружения

Убедитесь, что вы находитесь в корневой директории проекта:

```bash
cd "/Users/dmitriisirinov/Desktop/нгту/hakaton ml"
```

Проверьте версию Python (должна быть 3.9+):

```bash
python3 --version
```

---

## 📋 ЧАСТЬ 1: Подготовка данных (Никита)

### Шаг 1: Запуск подготовки данных

Этот скрипт выполнит все 8 задач Никиты:
- Анализ data.csv
- Присоединение marking.csv
- Удаление студентов теста
- Проверка отсутствующих данных
- Построение безопасных фич
- Создание признаков
- Проверка на утечки
- Подготовка финальных датасетов

```bash
python3 src/data_preparation.py
```

**Ожидаемый результат:**
- ✅ Созданы файлы:
  - `data/processed/train_final.parquet`
  - `data/processed/test_final.parquet`
  - `output/features_info.json`
- ✅ Логи сохранены в `output/logs.txt`

**Время выполнения:** ~5-10 минут (зависит от объема данных)

---

## 🤖 ЧАСТЬ 2: Обучение моделей (Дима)

### Шаг 2: Обучение Baseline модели

Начните с простой модели для понимания базовых метрик:

```bash
python3 src/baseline.py
```

**Ожидаемый результат:**
- ✅ Модель сохранена: `models/baseline.pkl`
- ✅ Метрики в логах: ROC-AUC, Accuracy, F1-score
- ✅ Информация сохранена: `output/baseline_info.json`

**Время выполнения:** ~1-2 минуты

---

### Шаг 3: Обучение CatBoost модели (основная)

Это основная модель для хакатона:

```bash
python3 src/model_catboost.py
```

**Ожидаемый результат:**
- ✅ Модель сохранена: `models/catboost_model.pkl`
- ✅ Метрики в логах (должны быть лучше baseline)
- ✅ Топ-10 важных признаков выведены в консоль
- ✅ Информация сохранена: `output/catboost_info.json`

**Время выполнения:** ~5-10 минут

---

### Шаг 4: Обучение LightGBM модели (опционально)

Альтернативная модель для сравнения:

```bash
python3 src/model_lightgbm.py
```

**Ожидаемый результат:**
- ✅ Модель сохранена: `models/lightgbm_model.pkl`
- ✅ Метрики в логах
- ✅ Информация сохранена: `output/lightgbm_info.json`

**Время выполнения:** ~5-10 минут

**Примечание:** Если LightGBM не работает (требуется libomp), можно пропустить этот шаг и использовать только CatBoost.

---

## 📊 ЧАСТЬ 3: Предсказания и Submission

### Шаг 5: Создание submission файла

Запустите inference для создания финального файла:

```bash
python3 src/inference.py
```

**Ожидаемый результат:**
- ✅ Файл создан: `output/submission.csv`
- ✅ Формат соответствует `sample_submission.csv`
- ✅ Статистика предсказаний в логах

**Время выполнения:** ~1 минута

---

## ✅ Проверка результатов

### Проверьте созданные файлы:

```bash
# Проверка моделей
ls -lh models/*.pkl

# Проверка submission
head output/submission.csv

# Проверка логов
tail -50 output/logs.txt
```

### Проверьте формат submission:

```bash
python3 -c "
import pandas as pd
sub = pd.read_csv('output/submission.csv')
print(f'✅ Строк: {len(sub)}')
print(f'✅ Колонки: {list(sub.columns)}')
print(f'✅ Формат правильный: {list(sub.columns) == [\"ID\", \"выпуск\"]}')
print(f'✅ Нет NaN: {sub.isnull().sum().sum() == 0}')
print(f'✅ Распределение:')
print(sub['выпуск'].value_counts())
"
```

---

## 🔄 Полный пайплайн одной командой

Если хотите запустить все сразу (не рекомендуется для первого раза):

```bash
# Подготовка данных
python3 src/data_preparation.py && \
# Baseline
python3 src/baseline.py && \
# CatBoost
python3 src/model_catboost.py && \
# Inference
python3 src/inference.py && \
echo "✅ Все шаги выполнены успешно!"
```

---

## ⚠️ Решение проблем

### Проблема: "Файл не найден: data/processed/train_final.parquet"

**Решение:** Запустите сначала `python3 src/data_preparation.py`

### Проблема: "LightGBM недоступен"

**Решение:** 
- Установите libomp: `brew install libomp`
- Или просто используйте CatBoost (он работает без проблем)

### Проблема: "ModuleNotFoundError"

**Решение:** Установите зависимости:
```bash
pip3 install -r requirements.txt
```

### Проблема: "Ошибка загрузки данных"

**Решение:** Проверьте, что файлы находятся в `data/raw:/` или `data/raw/`

---

## 📊 Ожидаемые метрики

После обучения моделей вы должны увидеть в логах примерно такие метрики:

**Baseline:**
- ROC-AUC: ~0.65-0.75
- Accuracy: ~0.70-0.80

**CatBoost:**
- ROC-AUC: ~0.75-0.85 (лучше baseline)
- Accuracy: ~0.75-0.85

**LightGBM:**
- ROC-AUC: ~0.75-0.85 (похоже на CatBoost)

---

## 🎯 Итоговый файл для отправки

После выполнения всех шагов ваш submission файл будет здесь:

```
output/submission.csv
```

Этот файл можно отправлять на хакатон!

---

## 📝 Чек-лист выполнения

- [ ] Шаг 1: Подготовка данных выполнена
- [ ] Шаг 2: Baseline модель обучена
- [ ] Шаг 3: CatBoost модель обучена
- [ ] Шаг 4: LightGBM модель обучена (опционально)
- [ ] Шаг 5: Submission файл создан
- [ ] Проверка формата submission пройдена
- [ ] Логи проверены на ошибки

---

## 🚀 Готово!

Теперь у вас есть:
- ✅ Обученные модели
- ✅ Файл submission.csv
- ✅ Логи всех операций
- ✅ Метрики моделей

**Удачи на хакатоне!** 🎉

