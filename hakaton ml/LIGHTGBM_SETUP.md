# Настройка LightGBM

## ✅ Автоматическая настройка

LightGBM теперь настраивается автоматически при импорте модулей проекта!

### Что сделано:

1. **Автоматическая настройка окружения** в `src/utils.py`
   - При импорте модулей автоматически настраиваются переменные окружения
   - Проверяется наличие libomp и настраиваются пути

2. **Обработка ошибок** в `src/model_lightgbm.py`
   - Проверка доступности LightGBM перед использованием
   - Понятные сообщения об ошибках
   - Рекомендации по установке libomp

3. **Интеграция в inference**
   - Автоматическая настройка при загрузке моделей

## 🚀 Использование

Просто запускайте скрипты как обычно:

```bash
# Обучение LightGBM
python3 src/model_lightgbm.py

# Inference с LightGBM
python3 src/inference.py
```

Если libomp не установлен, вы получите понятное сообщение с инструкциями.

## 📋 Ручная установка (если нужно)

Если автоматическая настройка не работает:

```bash
# Установка libomp
brew install libomp

# Проверка
python3 -c "import lightgbm; print('✅ LightGBM работает!')"
```

## ⚠️ Troubleshooting

### Ошибка: "Library not loaded: @rpath/libomp.dylib"

**Решение**: Установите libomp:
```bash
brew install libomp
```

### Ошибка: "LightGBM недоступен"

**Решение**: 
1. Убедитесь, что libomp установлен: `brew list libomp`
2. Перезапустите терминал
3. Попробуйте запустить снова

### Альтернатива

Если LightGBM не работает, используйте CatBoost:
```bash
python3 src/model_catboost.py
```

CatBoost работает без дополнительных настроек и часто показывает похожие результаты.

## ✅ Статус

**LightGBM полностью интегрирован и настроен автоматически!**

Все скрипты теперь автоматически настраивают окружение для LightGBM при запуске.

