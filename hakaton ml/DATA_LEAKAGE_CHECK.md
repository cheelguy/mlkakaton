# Отчет о проверке на даталики (Data Leakage)

## ✅ РЕЗУЛЬТАТ: ДАТАЛИКОВ НЕ ОБНАРУЖЕНО

---

## 📋 ПРАВИЛА (что можно / нельзя)

### ✅ МОЖНО использовать:
- Все данные до конца второго курса (семестры 1-4)
- Оценки за семестры 1-4
- Количество пересдач в этих 4 семестрах
- Статус студента в этот период (учится/академ)
- Долги
- Посещаемость
- Средний балл
- Динамику оценок
- Что угодно, что произошло ДО окончания второго курса

### ❌ НЕЛЬЗЯ использовать (даталики):
- Признаки, которые отражают будущее
- Произошли после 2 курса
- Зависят от итога обучения
- Имеют утечки

**Запрещенные примеры:**
- Статус студента после 2 курса
- Финальный статус из marking (кроме таргета)
- Долгов больше, чем было на тот момент
- Оценки из 5-6 семестров
- Дата отчисления, если она позже 2 курса
- Любые поля, которые невозможно знать заранее

---

## 🔍 ПРОВЕРКА НАШЕГО КОДА

### 1. ✅ ФИЛЬТРАЦИЯ ПО СЕМЕСТРАМ

**Код:** `data_preparation.py:159-161`, `split_train_test.py:52-57`

```python
# Используем только семестры 1-4
data_safe = data_df[data_df['SEMESTER'] <= 4].copy()
train_data = data_df[
    (data_df['PK'].isin(train_student_ids)) & 
    (data_df['SEMESTER'] <= 4)
].copy()
```

**Результат:**
- ✅ Все записи за семестры 1-4: 36,743
- ✅ Записей за семестры >4: 0
- ✅ Гарантируется, что используем только данные до конца 2 курса

---

### 2. ✅ ИСПОЛЬЗОВАНИЕ marking.csv

**Код:** `feature_engineering.py:199-203`

```python
# Объединяем ТОЛЬКО безопасные колонки
merged_df = features_df.merge(
    marking_latest[['student_id', 'Факультет', 'Направление', 'год поступления']],
    on='student_id',
    how='left'
)
```

**Что используем:**
- ✅ **Факультет** - можно (известен до обучения)
- ✅ **Направление** - можно (известно до обучения)
- ✅ **Год поступления** - можно (известен до обучения)

**Что НЕ используем:**
- ❌ **статус** - НЕ используем (может быть после 2 курса)
- ❌ **дата изменения** - НЕ используем (может быть после 2 курса)
- ❌ **выпуск** - НЕ используем в признаках (только таргет)

**Результат:** ✅ Используем только безопасные поля

---

### 3. ✅ ПРИЗНАКИ ПО СЕМЕСТРАМ

**Код:** `feature_engineering.py:142-152`

```python
# Средние оценки по каждому семестру
for sem in [1, 2, 3, 4]:  # ТОЛЬКО семестры 1-4!
    sem_data = student_data[student_data['semester'] == sem]
    features[f'semester_{sem}_score_mean'] = ...
    features[f'semester_{sem}_count'] = ...
```

**Результат:**
- ✅ Используются только семестры 1, 2, 3, 4
- ✅ Нет признаков для семестров 5, 6, 7, 8
- ✅ Все признаки основаны на данных до конца 2 курса

---

### 4. ✅ ДАТЫ ЭКЗАМЕНОВ

**Код:** `preprocess.py:28-33`

```python
if 'EXAM_DATE' in df.columns:
    df['EXAM_DATE'] = pd.to_datetime(df['EXAM_DATE'], errors='coerce')
    df['year'] = df['EXAM_DATE'].dt.year
    df['month'] = df['EXAM_DATE'].dt.month
    df['day_of_week'] = df['EXAM_DATE'].dt.dayofweek
```

**Проверка:**
- ✅ Даты используются только для создания временных признаков (год, месяц, день недели)
- ✅ Эти даты относятся только к экзаменам за семестры 1-4 (гарантируется фильтрацией)
- ✅ НЕ используем дату отчисления или другие даты после 2 курса

**Результат:** ✅ Безопасно

---

### 5. ✅ ФИНАЛЬНЫЕ ПРИЗНАКИ (38 признаков)

**Проверка всех признаков:**

**Статистики по оценкам (7 признаков):**
- ✅ `score_mean`, `score_median`, `score_min`, `score_max`, `score_std`, `score_count`, `score_sum`
- ✅ Все основаны на оценках за семестры 1-4

**Статистики по семестрам (11 признаков):**
- ✅ `semester_count`, `semester_min`, `semester_max`, `semester_score_mean`
- ✅ `semester_1_score_mean`, `semester_1_count`
- ✅ `semester_2_score_mean`, `semester_2_count`
- ✅ `semester_3_score_mean`, `semester_3_count`
- ✅ `semester_4_score_mean`, `semester_4_count`
- ✅ Все только для семестров 1-4

**Динамика (2 признака):**
- ✅ `score_trend` - динамика между семестрами 1-4
- ✅ `score_improvement` - улучшение между семестрами 1-4

**Типы оценок (6 признаков):**
- ✅ `exams_count`, `credits_count`, `practices_count`, `courseworks_count`
- ✅ `exam_score_mean`, `credit_score_mean`
- ✅ Все за семестры 1-4

**Дисциплины (2 признака):**
- ✅ `disciplines_count`, `records_count`
- ✅ Только за семестры 1-4

**Временные признаки (3 признака):**
- ✅ `year_min`, `year_max`, `year_span`
- ✅ Только даты экзаменов за семестры 1-4

**Процент успешных (3 признака):**
- ✅ `success_rate`, `excellent_rate`, `fail_rate`
- ✅ Только за семестры 1-4

**Информация о студенте (3 признака):**
- ✅ `faculty_encoded`, `direction_encoded`, `admission_year`
- ✅ Все известны до начала обучения

**Результат:** ✅ Все 38 признаков безопасны!

---

## ⚠️ ПОТЕНЦИАЛЬНЫЕ ПРОБЛЕМЫ (проверены)

### 1. "Последняя запись" в marking.csv

**Проблема:** В `merge_with_marking` мы берем последнюю запись по 'дата изменения'

**Проверка:**
- ✅ Мы используем только: Факультет, Направление, год поступления
- ✅ Эти данные НЕ меняются со временем (известны до обучения)
- ✅ Даже если последняя запись после 2 курса, эти поля остаются теми же

**Результат:** ✅ Безопасно

---

### 2. Даты экзаменов

**Проблема:** Используем даты для создания признаков

**Проверка:**
- ✅ Даты относятся только к экзаменам за семестры 1-4
- ✅ Фильтрация по семестрам гарантирует это
- ✅ НЕ используем дату отчисления или другие даты после 2 курса

**Результат:** ✅ Безопасно

---

## 📊 ИТОГОВАЯ ПРОВЕРКА

### Автоматическая проверка (`utils.py:check_data_leakage`)

**Найдено предупреждений:** 16

**Тип предупреждений:**
- Некоторые значения признаков в тесте отсутствуют в трейне
- Разные распределения признаков между train и test

**Анализ:**
- ⚠️ Это НЕ даталики, а нормальная ситуация
- Новые студенты в тесте могут иметь другие значения признаков
- Это ожидаемо и не является утечкой данных

**Результат:** ✅ Предупреждения не критичны

---

## ✅ ВЫВОДЫ

### Что мы используем (все безопасно):

1. ✅ **Оценки за семестры 1-4** - можно
2. ✅ **Статистики по оценкам** - можно
3. ✅ **Динамика оценок по семестрам 1-4** - можно
4. ✅ **Типы оценок (экзамены, зачеты)** - можно
5. ✅ **Количество дисциплин** - можно
6. ✅ **Процент успешных оценок** - можно
7. ✅ **Факультет, направление, год поступления** - можно (известны до обучения)
8. ✅ **Даты экзаменов (только за семестры 1-4)** - можно

### Что мы НЕ используем (запрещено):

1. ❌ **Статус студента** - НЕ используем
2. ❌ **Дата изменения** - НЕ используем
3. ❌ **Финальный статус** - НЕ используем (кроме таргета)
4. ❌ **Оценки за семестры >4** - НЕ используем
5. ❌ **Дата отчисления** - НЕ используем
6. ❌ **Любые данные после 2 курса** - НЕ используем

---

## 🎯 ФИНАЛЬНЫЙ ВЕРДИКТ

**✅ ДАТАЛИКОВ НЕ ОБНАРУЖЕНО!**

Все признаки соответствуют правилам:
- ✅ Используются только данные до конца 2 курса (семестры 1-4)
- ✅ Не используются запрещенные поля из marking.csv
- ✅ Не используются данные после 2 курса
- ✅ Все признаки можно было знать в момент конца 2 курса

**Проект готов к использованию!** 🚀

