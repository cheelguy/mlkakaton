# Workflow проекта: Разделение задач

## 👥 Команда

- **Никита (Data + Features)**: Подготовка данных и создание признаков
- **Дима (Models + Validation)**: Обучение моделей и валидация

---

## 📋 ЧАСТЬ 1: Подготовка данных (Никита)

### Задачи Никиты:

1. ✅ Проанализировать data.csv
2. ✅ Присоединить marking.csv
3. ✅ Удалить студентов теста (выпуск=None) для обучения
4. ✅ Проверить отсутствующие данные
5. ✅ Построить список безопасных фич
6. ✅ Построить сами фичи
7. ✅ Проверить на утечки
8. ✅ Передать финальный датасет Диме

### Запуск подготовки данных:

```bash
python src/data_preparation.py
```

### Результат работы Никиты:

После выполнения скрипта будут созданы файлы:
- `data/processed/train_final.parquet` - финальный train датасет
- `data/processed/test_final.parquet` - финальный test датасет
- `output/features_info.json` - информация о признаках

**Эти файлы передаются Диме для обучения моделей.**

---

## 📋 ЧАСТЬ 2: Обучение моделей (Дима)

### Задачи Димы:

1. ✅ Разделить данные train/test по логике "до/после 2 курса"
2. ✅ Построить baseline (логистическая регрессия)
3. ✅ Обучить CatBoost / LightGBM
4. ✅ Настроить валидацию
5. ✅ Проверить признаки на даталики
6. ✅ Прогнать тестовую выборку
7. ✅ Выгрузить результат в sample_submission.csv формат

### Запуск обучения моделей:

**Вариант 1: Использовать финальные датасеты от Никиты (рекомендуется)**

Если Никита уже подготовил данные, просто запускайте:

```bash
# Baseline
python src/baseline.py

# CatBoost
python src/model_catboost.py

# LightGBM
python src/model_lightgbm.py

# Inference
python src/inference.py
```

**Вариант 2: Полный пайплайн с нуля**

Если нужно запустить все с нуля:

```bash
# 1. Загрузка данных
python src/load_data.py

# 2. Предобработка
python src/preprocess.py

# 3. Feature engineering
python src/feature_engineering.py

# 4. Разделение train/test
python src/split_train_test.py

# 5. Обучение моделей
python src/baseline.py
python src/model_catboost.py
python src/model_lightgbm.py

# 6. Inference
python src/inference.py
```

### Результат работы Димы:

- `models/baseline.pkl` - baseline модель
- `models/catboost_model.pkl` - CatBoost модель
- `models/lightgbm_model.pkl` - LightGBM модель
- `output/submission.csv` - финальный submission файл

---

## 🔄 Полный workflow (оба участника)

### Шаг 1: Никита готовит данные

```bash
python src/data_preparation.py
```

**Проверка:**
- ✅ Файлы `train_final.parquet` и `test_final.parquet` созданы
- ✅ Логи в `output/logs.txt` показывают успешное завершение
- ✅ Нет предупреждений о даталиках

### Шаг 2: Дима обучает модели

```bash
# Baseline
python src/baseline.py

# CatBoost (основная модель)
python src/model_catboost.py

# LightGBM (альтернатива)
python src/model_lightgbm.py
```

**Проверка:**
- ✅ Модели сохранены в `models/`
- ✅ Метрики в логах показывают хорошие результаты
- ✅ ROC-AUC > 0.7 (желательно)

### Шаг 3: Дима делает предсказания

```bash
python src/inference.py
```

**Проверка:**
- ✅ `output/submission.csv` создан
- ✅ Формат соответствует `sample_submission.csv`
- ✅ Нет NaN значений
- ✅ Все ID из теста присутствуют

---

## 📊 Структура данных

### Входные данные (raw):
- `data/raw/data.csv` - данные об оценках
- `data/raw/marking.csv` - информация о студентах
- `data/raw/sample_submission.csv` - формат submission

### Промежуточные данные (processed):
- `data/processed/data_raw.parquet` - сырые данные
- `data/processed/data_processed.parquet` - обработанные данные
- `data/processed/features.parquet` - признаки (старый способ)
- `data/processed/train_final.parquet` - **финальный train (от Никиты)**
- `data/processed/test_final.parquet` - **финальный test (от Никиты)**

### Результаты:
- `models/*.pkl` - обученные модели
- `output/submission.csv` - финальный submission
- `output/logs.txt` - все логи
- `output/*_info.json` - метаданные моделей

---

## ⚠️ Важные моменты

### Для Никиты:
1. **Безопасные фичи**: Используйте только данные за семестры 1-4
2. **Проверка утечек**: Обязательно запустите проверку перед передачей данных
3. **Качество данных**: Убедитесь, что нет критических пропусков

### Для Димы:
1. **Валидация**: Всегда проверяйте метрики на валидационной выборке
2. **Даталики**: Проверяйте признаки перед обучением
3. **Формат submission**: Строго следуйте формату sample_submission.csv

---

## 🐛 Troubleshooting

### Ошибка: "Финальный датасет не найден"
**Решение**: Запустите `python src/data_preparation.py` сначала

### Ошибка: "Модель не найдена"
**Решение**: Обучите модель перед inference

### Ошибка: "Несовпадение признаков"
**Решение**: Убедитесь, что train и test используют одинаковые признаки

---

## 📝 Чек-лист перед сдачей

### Никита:
- [ ] `train_final.parquet` создан и содержит все нужные признаки
- [ ] `test_final.parquet` создан и содержит те же признаки
- [ ] Проверка на утечки пройдена без критических предупреждений
- [ ] Логи сохранены в `output/logs.txt`

### Дима:
- [ ] Baseline модель обучена и метрики записаны
- [ ] CatBoost модель обучена (лучшая из всех)
- [ ] LightGBM модель обучена (опционально)
- [ ] `submission.csv` создан и проверен на формат
- [ ] Все метрики записаны в логи

---

## 🎯 Итоговый результат

Финальный файл для отправки:
- `output/submission.csv` - готовый submission файл

Формат:
```csv
ID,выпуск
116556,1
116591,0
...
```

Где `выпуск` = 1 (выпустился) или 0 (отчислен)

